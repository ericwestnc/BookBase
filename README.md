# BookBase

BookBase is a cross-platform .NET MAUI personal library manager built with MVVM, SQLite, and dependency injection.

## Build

```bash
dotnet build /home/runner/work/BookBase/BookBase/src/BookBase/BookBase.csproj -f net9.0-android
```
